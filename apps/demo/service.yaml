apiVersion: v1
kind: Service
metadata:
  name: demo
  namespace: demo
  labels:
    app.kubernetes.io/name: demo
    app.kubernetes.io/component: normal-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
