apiVersion: v1
kind: Service
metadata:
  name: demo-isolated
  namespace: demo-isolated
  labels:
    app.kubernetes.io/name: demo-isolated
    app.kubernetes.io/component: normal-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo-isolated
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
